# Neatinates the RIFT's xz coordinates by centring it on the block it's on

execute as @e[type=armor_stand,tag=exterior_RIFT] at @s align xz run tp ~.5 ~ ~.5

scoreboard players set neatinatorButton RIFTcontrols 1